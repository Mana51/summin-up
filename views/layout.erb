<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>Summin-up</title>
    <link rel="stylesheet" href="/css/original.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Megrim&family=Montserrat&family=Zen+Kaku+Gothic+New&display=swap" rel="stylesheet">
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <link rel="shortcut icon" href="/favicon.ico">
</head>
<body>
    <nav>
        <div class="navbar">
            <div class="nav-left">
                <a id="title" <% if session[:user] %> href="/home/newest" <% end %>>Summin-up</a>
                <% if session[:user] %><% if @page=="home" or @page=="filtered" %><a class="generateButton nav-left" href="/generate">Generate article</a><% end %><% end %>
            </div>
            <div class="nav-right">
                <% unless session[:user] %>
                    <% if @page=="signin" %>
                        <a class="account" href="/signup">Sign up</a>
                    <% elsif @page=="signup" %>
                        <a class="account" href="/signin">Log in</a> 
                    <% end %>
                <% else %>
                    <% if @page=="home" or @page=="filtered" %>
                        <a class="libraryButton" href="/library">Library</a>    
                    <% end %>
                    <a class="account" href="/signout">Log out</a>
                <% end %> 
            </div>
        </div>
    </nav>
    
    <%= yield %>
</body>
</html>